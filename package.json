{
  "name": "admin-panel",
  "version": "1.0.0",
  "description": "Admin Panel — runs server (port 5001) and client (port 3002) together",
  "scripts": {
    "start": "concurrently \"npm start --prefix server\" \"npm start --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "server": "npm start --prefix server",
    "client": "npm start --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
